import { CalendarTheme } from '../types'

/**
 * @constant
 * The default theme settings for the calendar component.
 * @default
 */
export const CALENDAR_THEME: CalendarTheme = {
  isDark: false,
  general: {
    fontSizeIcon: 14,
  },
  custom: {
    buttonBorderRadius: 50,
  },
}
